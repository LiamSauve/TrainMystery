using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CharacterData : MonoBehaviour
{
    //convname, convdesc, convhand
    public string[] dialogueStrings = { "Poetian", "The old war eagle. A legendary veteran, turned combat instructor to the Prince. He's never seen without a cigar and his trusty sabre.", "LEFT" };

    void Start()
    {
        
    }
}
